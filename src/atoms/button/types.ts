export type TButton = {
    label: string;
    backgroundColor?: string;
    textColor?: string;
    borderRadius?: string;
    padding: string;
    fontSize: string;
    fontWeight: string;
    fontFamily?: string;
    onClick: () => void;
};
